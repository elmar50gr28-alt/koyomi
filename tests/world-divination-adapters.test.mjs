import assert from'node:assert/strict';
import{evaluateWorldDivinations}from'../src/world/multi-mundane/world-divination-adapters.js';
const ephemeris={planetLongitudes(date){const d=date.getUTCDate();return{Sun:(120+d)%360,Moon:(40+d*13)%360,Mercury:190,Mars:10+d,Saturn:100,Uranus:280,Neptune:350,Pluto:190}}},base={date:new Date('2026-09-01T12:00:00Z'),latitude:35.68,longitude:139.76,ephemeris,horizonDays:30},a=evaluateWorldDivinations(base),b=evaluateWorldDivinations(base);
assert.deepEqual(a,b);assert.equal(a.schemaId,'koyomi-role-based-mundane-v1');assert.deepEqual(a.systems.map(x=>x.systemId),['western-mundane','ganzhi-world','sukuyo-world','kyusei-world','qimen-world']);assert.ok(!('averageScore'in a));assert.ok(!('agreementCount'in a));assert.ok(a.regionSelection.score>=0&&a.regionSelection.score<=100);assert.ok(a.regionSelection.periodAverage>=0&&a.regionSelection.periodAverage<=100);assert.ok(a.regionSelection.strongestRunDays>=0&&a.regionSelection.strongestRunDays<=31);assert.match(a.regionSelection.peakDate,/^2026-09-/);assert.ok(['近い時期に重なる','同じ週に重なる','時期は分散'].includes(a.timingSupport.agreement));assert.equal(a.directionSupport.status,'unavailable');assert.equal(a.systems[3].score,null);assert.equal(a.systems[4].score,null);assert.equal(a.systems[3].status,'unavailable');
const directed=evaluateWorldDivinations({...base,directionOrigin:{latitude:35.68,longitude:135}});assert.equal(directed.directionSupport.status,'ready');assert.ok(Number.isFinite(directed.directionSupport.bearing));assert.ok(directed.systems.slice(3).every(x=>x.status==='ready'&&Number.isFinite(x.score)));
const shifted=evaluateWorldDivinations({...base,date:new Date('2026-09-02T12:00:00Z')});assert.notEqual(a.calculationTimeUtc,shifted.calculationTimeUtc);console.log('Role-based world divination engine passed');
