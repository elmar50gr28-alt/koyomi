import assert from'node:assert/strict';import{readFile}from'node:fs/promises';import vm from'node:vm';const c={};vm.runInNewContext(await readFile('src/persona/sister-lexicon.js','utf8'),c);const x=c.KOYOMI_SISTER_LEXICON;for(const g of['transition','forward','trial','defense','evidence','action','closing'])assert.ok(x.size(g)>=4);const a=x.session('same'),p1=a.pick('transition'),p2=a.pick('transition',1);assert.notEqual(p1,p2);assert.equal(x.session('same').pick('transition'),p1);x.register('work',['期限と担当を決めなさい。']);assert.equal(x.size('work'),1);console.log('Sister lexicon passed');
