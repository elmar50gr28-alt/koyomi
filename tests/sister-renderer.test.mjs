import assert from'node:assert/strict';import{readFile}from'node:fs/promises';import vm from'node:vm';const c={};vm.runInNewContext(await readFile('src/persona/sister-renderer.js','utf8'),c);const r=c.KOYOMI_PERSONA_RENDERER,m={system:'タロット',opening:'あら、見えたわ。',axis:'結果だけを見るわよ。',result:'選択を一つに絞る時よ。',scenario:{scene:'目前の選択が具体化しやすい',observable:'相手の反応',action:'条件を一つ確認する',go:'期限が決まること',stop:'約束の不履行',review:'7日'},evidence:['最終結果 正位置'],closing:'確認してから進みなさい。'};const text=r.render(m);for(const x of['姐さんの見立て','まず、これをおやりなさい','ここで止まりなさい','根拠はここよ'])assert.ok(text.includes(x));r.registerVoice('test',{...r.listVoices(),result:'試験結論'});assert.ok(r.listVoices().includes('test'));r.registerSection('extra',()=> '【拡張】\n追加');assert.ok(r.render({...m,order:['opening','extra']}).includes('【拡張】'));assert.ok(r.render(m,{voice:'zubat'}).includes('ズバッと結論'));console.log('Sister renderer passed');
