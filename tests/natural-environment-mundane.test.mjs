import assert from'node:assert/strict';import{calculateNaturalEnvironmentSign,forecastNaturalEnvironment}from'../src/world/natural-environment/natural-environment-core.js';import{scanNaturalEnvironment}from'../src/world/natural-environment/natural-environment-scanner.js';const ephemeris={planetLongitudes(date){const d=date.getUTCDate();return{Sun:d,Moon:90+d,Mercury:180+d,Mars:180+d,Jupiter:270,Saturn:0,Uranus:270+d,Neptune:90}}},date=new Date('2026-09-01T00:00:00Z'),args={startDate:date,placeId:'x',placeNameJa:'西日本周辺',latitude:35,longitude:135,ephemeris,horizonDays:7},a=forecastNaturalEnvironment(args),b=forecastNaturalEnvironment(args);assert.deepEqual(a,b);assert.equal(a.category,'natural-environment');assert.match(a.reading,/西日本周辺/);assert.ok(a.eventCandidates.length<=3);assert.ok(a.eventCandidates.every(x=>x.evidence.length));assert.equal(a.officialStatus.status,'not-connected');assert.match(a.officialStatus.message,/警報がないことを意味しません/);const sign=calculateNaturalEnvironmentSign({date,latitude:35,longitude:135,positions:ephemeris.planetLongitudes(date)});assert.ok(sign.score>=0&&sign.score<=100);const scan=scanNaturalEnvironment({startDate:date,ephemeris,horizonDays:1,limit:3});assert.equal(scan.ranked.length,3);assert.equal(scan.cells.length,scan.scannedCellCount);assert.equal(scan.cells[0].score>=scan.cells.at(-1).score,true);assert.equal(scan.selectionUsedObservations,false);assert.ok(scan.scannedCellCount>=700);const local=scanNaturalEnvironment({startDate:date,ephemeris,horizonDays:1,limit:2,cells:[{cellId:'a',latitude:35,longitude:135},{cellId:'b',latitude:36,longitude:136}]});assert.equal(local.cells.length,2);console.log('Natural environment mundane deterministic scan passed');
