(function(root){
'use strict';
const esc=value=>String(value).replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));
const point=(lon,r,cx=180,cy=180)=>{const a=(Number(lon)-90)*Math.PI/180;return{x:cx+Math.cos(a)*r,y:cy+Math.sin(a)*r}};
function render(result){const p=result?.natal?.placements||{},cusps=result?.natal?.cusps||[],aspects=result?.natal?.aspects||[],known=result?.professional?.confidence!=='limited',lines=[];for(let i=0;i<12;i++){const q=point(i*30,160);lines.push(`<line x1="180" y1="180" x2="${q.x.toFixed(2)}" y2="${q.y.toFixed(2)}" class="sign-line"/>`)}if(known)cusps.forEach((lon,i)=>{const q=point(lon,130);lines.push(`<line x1="180" y1="180" x2="${q.x.toFixed(2)}" y2="${q.y.toFixed(2)}" class="house-line"/><text x="${point(lon+4,118).x.toFixed(2)}" y="${point(lon+4,118).y.toFixed(2)}" class="house-label">${i+1}</text>`)});const bodies=Object.entries(p),bodyMap=new Map();bodies.forEach(([name,v],i)=>{const q=point(v.lon,98-i%3*8);bodyMap.set(name,q);lines.push(`<circle cx="${q.x.toFixed(2)}" cy="${q.y.toFixed(2)}" r="10" class="planet-dot"/><text x="${q.x.toFixed(2)}" y="${(q.y+4).toFixed(2)}" class="planet-label">${esc(name.slice(0,1))}</text>`)});aspects.slice(0,24).forEach(a=>{const x=bodyMap.get(a.a),y=bodyMap.get(a.b);if(x&&y)lines.unshift(`<line x1="${x.x.toFixed(2)}" y1="${x.y.toFixed(2)}" x2="${y.x.toFixed(2)}" y2="${y.y.toFixed(2)}" class="aspect ${a.name==='スクエア'||a.name==='オポジション'?'hard':'soft'}"/>`)});return`<svg class="western-chart-wheel" viewBox="0 0 360 360" role="img" aria-label="西洋占星術チャートホイール"><style>.western-chart-wheel{max-width:460px;width:100%;background:#fff;border:3px solid #111;border-radius:50%}.western-chart-wheel .sign-line{stroke:#bbb;stroke-width:1}.western-chart-wheel .house-line{stroke:#222;stroke-width:1.4}.western-chart-wheel .aspect{stroke-width:1;opacity:.48}.western-chart-wheel .hard{stroke:#b23}.western-chart-wheel .soft{stroke:#16805c}.western-chart-wheel .planet-dot{fill:#fff;stroke:#111;stroke-width:2}.western-chart-wheel text{font:700 10px sans-serif;text-anchor:middle}.western-chart-wheel .house-label{font-size:8px}</style><circle cx="180" cy="180" r="160" fill="none" stroke="#111" stroke-width="3"/><circle cx="180" cy="180" r="130" fill="none" stroke="#555"/>${lines.join('')}</svg>`}
function build(result){return{available:Object.keys(result?.natal?.placements||{}).length>0,svg:render(result),birthTimeKnown:result?.professional?.confidence!=='limited'}}
root.WesternChartWheelV1={build,render};
})(globalThis);
