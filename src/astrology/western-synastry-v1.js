(function(root){'use strict';function build(core,input={}){const items=input.aspects||[];const supportive=items.filter(x=>['合','セクスタイル','トライン'].includes(x.name)).length;const challenging=items.filter(x=>['スクエア','オポジション'].includes(x.name)).length;return{available:Boolean(input.partner),items,score:Math.max(0,Math.min(100,50+supportive*6-challenging*4)),note:input.partner?'二人の天体間アスペクトを比較':'相手の出生情報を入力すると表示'}}root.WesternSynastryV1={build};})(globalThis);
