(function(root){'use strict';function build(core){const p=core?.natal?.placements||{},bySign={};Object.entries(p).forEach(([name,x])=>(bySign[x.sign]??=[]).push(name));const patterns=[];Object.entries(bySign).forEach(([sign,names])=>{if(names.length>=3)patterns.push({type:'ステリウム',sign,members:names})});return{items:patterns,note:'複合パターンは成立条件を満たすものだけ表示'}}root.WesternPatternsV1={build};})(globalThis);
