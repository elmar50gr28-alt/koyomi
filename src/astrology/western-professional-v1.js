(function(root){'use strict';function build(core,input={}){const known=core?.calculationMeta?.birthTimeKnown!==false;return{houseSystem:known?(input.houseSystem||'equal'):'suppressed',zodiac:'tropical',rulership:'modern',confidence:known?'standard':'limited',accuracyClass:known?'timed':'date-only',warnings:known?[]:['出生時刻不明のためASC・MC・ハウス・時刻依存感受点を除外しました'],suppressed:core?.calculationMeta?.suppressed||[],offlineCapable:true}}root.WesternProfessionalV1={build};})(globalThis);
