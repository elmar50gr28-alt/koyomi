import { forecastConflictSign } from '../conflict/conflict-signs-core.js';
import { generateBlindWorldCells } from './blind-world-grid.js';
import { selectSpatiallyDiverse } from './spatial-clusterer.js';
import { resolveBlindPlace } from './world-place-resolver.js';
import { inspectBlindScanInput } from './selection-bias-guard.js';
export const BLIND_MUNDANE_VERSION='blind-world-mundane-v1.0';
export function scanBlindWorld(input={}){const audit=inspectBlindScanInput(input);if(!audit.ok)throw new TypeError(`blind scan rejects observational inputs: ${audit.forbidden.join(',')}`);const{startDate=new Date(),ephemeris,horizonDays=30,limit=5}=input;if(!ephemeris?.planetLongitudes)throw new TypeError('ephemeris is required');const cells=generateBlindWorldCells(),cache=new Map(),cached={...ephemeris,planetLongitudes(date){const key=date.toISOString();if(!cache.has(key))cache.set(key,ephemeris.planetLongitudes(date));return cache.get(key)}},scored=cells.map(cell=>forecastConflictSign({startDate,placeId:cell.cellId,placeNameJa:'名称付与前の世界格子',latitude:cell.latitude,longitude:cell.longitude,ephemeris:cached,horizonDays})).sort((a,b)=>b.score-a.score||a.placeId.localeCompare(b.placeId)),diverse=selectSpatiallyDiverse(scored,{limit,minDistanceKm:1400}),ranked=diverse.map(item=>forecastConflictSign({startDate,placeId:item.placeId,placeNameJa:resolveBlindPlace(item),latitude:item.latitude,longitude:item.longitude,ephemeris:cached,horizonDays})),controls=[...scored].sort((a,b)=>a.score-b.score||a.placeId.localeCompare(b.placeId)).slice(0,limit).map(item=>({cellId:item.placeId,score:item.score,latitude:item.latitude,longitude:item.longitude}));return Object.freeze({schemaId:'koyomi-blind-world-scan-v1',engineVersion:BLIND_MUNDANE_VERSION,issuedAt:new Date(startDate).toISOString(),gridResolution:1,scannedCellCount:cells.length,selectionUsedObservations:false,ranked:Object.freeze(ranked),controls:Object.freeze(controls)})}
