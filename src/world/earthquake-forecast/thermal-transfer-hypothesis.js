export const THERMAL_TRANSFER_HYPOTHESIS_VERSION='thermal-transfer-hypothesis-v1';
const component=(status,value,reason)=>Object.freeze({status,value:value==null||!Number.isFinite(Number(value))?null:Number(value),reason:String(reason||'')});
export function evaluateThermalTransferHypothesis({geologicalBackground=null,depthMigration=null,surfaceTemperature=null,persistence=null,sourceExclusions=null,spatialMatch=null,temporalMatch=null,dataQuality=null}={}){const surfaceDeviation=surfaceTemperature?.monthlyTimeBaselineDeviationC,persistenceValue=persistence?.value??(surfaceTemperature?.persistenceDays==null?null:Math.min(1,Number(surfaceTemperature.persistenceDays)/3)),components=Object.freeze({geologicalBackground:component(geologicalBackground?.status,geologicalBackground?.value,geologicalBackground?.reason),depthMigration:component(depthMigration?.status,depthMigration?.strength,depthMigration?.direction),surfaceTemperature:component(surfaceTemperature?.status,surfaceDeviation==null?null:Math.min(1,Math.abs(Number(surfaceDeviation))/5),surfaceTemperature?.reason),persistence:component(persistence?.status,persistenceValue,persistence?.reason),sourceExclusions:component(sourceExclusions?.status,sourceExclusions?.value,sourceExclusions?.reason),dataQuality:component(dataQuality?.status,dataQuality?.value,dataQuality?.reason),spatialMatch:component(spatialMatch?.status,spatialMatch?.value,spatialMatch?.reason),temporalMatch:component(temporalMatch?.status,temporalMatch?.value,temporalMatch?.reason)}),required=Object.keys(components),ready=required.every(key=>components[key].status==='available'&&components[key].value!==null);if(!ready)return Object.freeze({version:THERMAL_TRANSFER_HYPOTHESIS_VERSION,status:'insufficient-data',label:'熱移送仮説との一致度',agreement:null,components,reason:surfaceTemperature?.status==='data-unavailable'?'地表温度データ未接続':'必要な観測または品質条件が不足しています',isProbability:false,scientificProbabilityContribution:0});const values=required.map(key=>components[key].value),agreement=Math.round(values.reduce((sum,value)=>sum+Math.max(0,Math.min(1,value)),0)/values.length*100);return Object.freeze({version:THERMAL_TRANSFER_HYPOTHESIS_VERSION,status:'available',label:'熱移送仮説との一致度',agreement,components,reason:'独立要素の研究上の一致であり、因果関係や発生確率を示しません',isProbability:false,scientificProbabilityContribution:0})}
