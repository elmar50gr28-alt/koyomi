export const THERMAL_OBSERVATION_QUALITY_VERSION='thermal-observation-quality-v1';
const parseTime=value=>{const time=Date.parse(value);return Number.isFinite(time)?time:null};
export function assessThermalObservationQuality(observation,{asOf=new Date(),maximumAgeHours=48,minimumConfidence=.6}={}){const cutoff=new Date(asOf).getTime(),time=parseTime(observation?.timeUtc??observation?.acquiredAt),label=String(observation?.confidence||'').toLowerCase(),confidence=typeof observation?.confidence==='number'?observation.confidence:label==='h'?1:label==='n'?0.7:label==='l'?0.3:null;if(!Number.isFinite(cutoff)||time===null||time>cutoff)return Object.freeze({status:'invalid',usable:false,stale:null,confidence:null,reason:'観測時刻を確認できません'});const stale=cutoff-time>maximumAgeHours*3_600_000,lowConfidence=confidence===null||confidence<minimumConfidence;return Object.freeze({status:stale?'stale':lowConfidence?'low-confidence':'usable',usable:!stale&&!lowConfidence,stale,confidence,ageHours:(cutoff-time)/3_600_000,reason:stale?'観測が古いため研究判定から除外':lowConfidence?'観測信頼度が低いため研究判定から除外':'観測品質条件を満たします'})}
