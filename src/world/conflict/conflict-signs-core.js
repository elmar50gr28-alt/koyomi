import { calculateAngles } from '../../mundane/western/seasonal-ingress-core.js';
import { normalizeMundanePositions } from '../mundane-earthquake-adapter.js';
import { inspectConflictReading } from './conflict-quality-guard.js';

export const CONFLICT_SIGNS_VERSION='conflict-signs-v1.0';
const LABELS={Sun:'太陽',Moon:'月',Mercury:'水星',Mars:'火星',Jupiter:'木星',Saturn:'土星',Uranus:'天王星',Neptune:'海王星',Pluto:'冥王星'};
const distance=(a,b)=>Math.abs((((a-b)+540)%360)-180),dateLabel=date=>`${date.getMonth()+1}月${date.getDate()}日`,bandFor=score=>score>=80?4:score>=65?3:score>=45?2:score>=25?1:0;
const aspect=(a,b,orb=6)=>{const separation=distance(a,b),hit=[0,90,180].map(angle=>({angle,orb:Math.abs(separation-angle)})).sort((x,y)=>x.orb-y.orb)[0];return hit.orb<=orb?hit:null};
const BANDS=['静かな巡り','緊張の芽','対立の兆し','軍事的緊張の兆し','大きな動乱の兆し'];
const SCENARIOS=Object.freeze({Mars:['軍事演習・部隊集結','越境や領空をめぐる応酬'],Saturn:['封鎖・制裁・長期的な圧力','停戦交渉の停滞'],Uranus:['無人機・ミサイルを伴う急な示威行動','サイバー・通信妨害'],Neptune:['情報戦・秘密工作をめぐる混乱','代理勢力を通じた衝突'],Pluto:['権力をめぐる強硬な駆け引き','対立構造の大きな組み替え'],Mercury:['強硬声明と外交交渉の応酬','通信・情報面での圧力'],Jupiter:['同盟国や国際機関の関与','対立範囲の拡大']});
const TARGETS=Object.freeze({Mars:['国境地帯','航空圏'],Saturn:['補給路','港湾・海上交通'],Uranus:['通信網','航空圏'],Neptune:['情報・通信分野','海上交通'],Pluto:['行政・統治機構','エネルギー供給網'],Mercury:['通信網','外交経路'],Jupiter:['同盟・国際関係','広域交通網']});

export function calculateConflictSign({date,latitude,longitude,positions}){const normalized=normalizeMundanePositions(positions),angles=calculateAngles(new Date(date),{latitude,longitude}),axes=[angles.ascendant,angles.midheaven,(angles.midheaven+180)%360],contributors=[];for(const [left,right,weight] of [['Mars','Saturn',20],['Mars','Uranus',23],['Mars','Pluto',24],['Saturn','Uranus',18],['Mercury','Mars',14],['Jupiter','Pluto',14],['Neptune','Mars',16]]){if(!Number.isFinite(normalized[left])||!Number.isFinite(normalized[right]))continue;const hit=aspect(normalized[left],normalized[right]);if(hit)contributors.push({body:left,id:`${left}-${right}`,label:`${LABELS[left]}と${LABELS[right]}の${hit.angle}度`,weight,orb:hit.orb})}for(const body of ['Mars','Saturn','Uranus','Neptune','Pluto']){if(!Number.isFinite(normalized[body]))continue;const orb=Math.min(...axes.map(axis=>distance(normalized[body],axis)));if(orb<=6)contributors.push({body,id:`angular-${body}`,label:`${LABELS[body]}が地域の主要軸付近`,weight:body==='Mars'||body==='Pluto'?22:16,orb})}const score=Math.min(100,Math.max(0,Math.round(12+contributors.reduce((sum,item)=>sum+Math.max(3,item.weight-item.orb*1.5),0)))),band=bandFor(score),ordered=contributors.sort((a,b)=>b.weight-a.weight),bodies=[...new Set(ordered.map(item=>item.body))],scenarios=[...new Set(bodies.flatMap(body=>SCENARIOS[body]||[]))].slice(0,3),targets=[...new Set(bodies.flatMap(body=>TARGETS[body]||[]))].slice(0,2),direction=bodies.includes('Pluto')||bodies.includes('Jupiter')?'拡大へ傾きやすい':bodies.includes('Saturn')?'膠着が続きやすい':bodies.includes('Mercury')?'交渉と応酬が交錯しやすい':'示威と牽制が中心になりやすい';return Object.freeze({score,band,label:BANDS[band],contributors:Object.freeze(ordered),scenarios:Object.freeze(scenarios.length?scenarios:['外交・軍事面での牽制']),targets:Object.freeze(targets.length?targets:['国境・周辺海域']),direction})}
export function forecastConflictSign({startDate=new Date(),placeId,placeNameJa,latitude,longitude,ephemeris,horizonDays=30}){const start=new Date(startDate);start.setUTCHours(12,0,0,0);const days=[];for(let offset=0;offset<=horizonDays;offset++){const date=new Date(start.getTime()+offset*86400000);days.push({date,sign:calculateConflictSign({date,latitude,longitude,positions:ephemeris.planetLongitudes(date)})})}days.sort((a,b)=>b.sign.score-a.sign.score||a.date-b.date);const peak=days[0],windowStart=new Date(Math.max(start.getTime(),peak.date.getTime()-86400000)),windowEnd=new Date(peak.date.getTime()+2*86400000),sign=peak.sign,reading=`${dateLabel(windowStart)}から${dateLabel(windowEnd)}、${placeNameJa}では${sign.label}が表面化しやすいでしょう。焦点は${sign.targets.join('と')}です。${sign.scenarios.join('、')}の順に兆しが強く、${sign.direction}流れです。${sign.contributors.slice(0,2).map(item=>item.label).join('、')||'天体の刺激はまだ穏やか'}が背景にあります。`,quality=inspectConflictReading(reading);return Object.freeze({schemaId:'koyomi-conflict-sign-v1',engineVersion:CONFLICT_SIGNS_VERSION,category:'conflict',placeId,placeNameJa,latitude,longitude,issuedAt:new Date(startDate).toISOString(),peakDate:peak.date.toISOString(),windowStart:windowStart.toISOString(),windowEnd:windowEnd.toISOString(),horizonDays,score:sign.score,band:sign.band,label:sign.label,targets:sign.targets,scenarios:sign.scenarios,direction:sign.direction,contributors:sign.contributors,reading,quality})}
export function rankConflictSigns({places,startDate,ephemeris,horizonDays=30,limit=5}){const cache=new Map(),cached={...ephemeris,planetLongitudes(date){const key=date.toISOString();if(!cache.has(key))cache.set(key,ephemeris.planetLongitudes(date));return cache.get(key)}};return places.map(place=>forecastConflictSign({...place,startDate,ephemeris:cached,horizonDays})).sort((a,b)=>b.score-a.score||a.placeNameJa.localeCompare(b.placeNameJa,'ja')).slice(0,limit)}
