const LEVELS=Object.freeze({unknown:{band:0,labelJa:'公式評価なし',color:'#707982',symbol:'?'},normal:{band:1,labelJa:'通常・活火山に留意',color:'#26845b',symbol:'○'},advisory:{band:2,labelJa:'注意',color:'#d5ad22',symbol:'△'},watch:{band:3,labelJa:'警戒',color:'#e27622',symbol:'▲'},warning:{band:4,labelJa:'重大警戒',color:'#c72f35',symbol:'!'}});
const JMA=Object.freeze({1:'normal',2:'advisory',3:'watch',4:'warning',5:'warning'}),USGS=Object.freeze({NORMAL:'normal',ADVISORY:'advisory',WATCH:'watch',WARNING:'warning'});
export const ALERT_MAX_AGE_HOURS=48;
export function normalizeOfficialAlert(record,{asOf=new Date()}={}){if(!record)return Object.freeze({...LEVELS.unknown,status:'unavailable',reason:'公的警戒情報を取得できません',sourceAgency:null,sourceUrl:null,issuedAt:null,stale:true});const system=String(record.system||'').toUpperCase(),key=system==='JMA'?JMA[Number(record.level)]:system==='USGS'?USGS[String(record.level||'').toUpperCase()]:null,issued=Date.parse(record.issuedAt),stale=!Number.isFinite(issued)||(Date.parse(asOf)-issued)>ALERT_MAX_AGE_HOURS*3600000;if(!key||stale)return Object.freeze({...LEVELS.unknown,status:stale?'stale':'unavailable',reason:stale?'公的情報の更新期限を過ぎています':'公的警戒レベルを確認できません',sourceAgency:record.sourceAgency||null,sourceUrl:record.sourceUrl||null,issuedAt:record.issuedAt||null,stale:true});return Object.freeze({...LEVELS[key],status:'available',reason:String(record.summaryJa||LEVELS[key].labelJa),sourceAgency:record.sourceAgency||system,sourceUrl:record.sourceUrl||null,issuedAt:record.issuedAt,stale:false})}
export function observationRing(thermal,seismic){if(thermal?.stale&&(Number(thermal?.detectionCount)||0)>0)return Object.freeze({band:4,color:'#8b7794',label:'古い衛星熱観測（参考）'});if(thermal?.multiSensorConfirmed)return Object.freeze({band:3,color:'#8139a8',label:'複数衛星で熱を確認'});if((Number(thermal?.detectionCount)||0)>0)return Object.freeze({band:2,color:'#31a7c9',label:'衛星熱を確認'});if((Number(seismic?.eventCount)||0)>0)return Object.freeze({band:1,color:'#3678c8',label:'周辺地震あり（参考）'});return Object.freeze({band:0,color:'#d8d0bf',label:'顕著な観測変化なし'})}
export const OFFICIAL_ALERT_LEVELS=LEVELS;
