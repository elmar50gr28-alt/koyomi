import { SNAPSHOT_SCHEMA_ID,WORLD_PREDICTION_ENGINE_VERSION } from './config.js';

export function createPredictionSnapshot({generatedAt=new Date().toISOString(),forecastStart,forecastEnd,modelVersion=WORLD_PREDICTION_ENGINE_VERSION,datasetVersion,cells=[]}={}){const generated=new Date(generatedAt),start=new Date(forecastStart),end=new Date(forecastEnd);if([generated,start,end].some(date=>Number.isNaN(date.getTime()))||end<=start)throw new RangeError('valid snapshot dates are required');if(!datasetVersion)throw new TypeError('datasetVersion is required');return Object.freeze({schemaId:SNAPSHOT_SCHEMA_ID,generatedAt:generated.toISOString(),forecastStart:start.toISOString(),forecastEnd:end.toISOString(),modelVersion:String(modelVersion),datasetVersion:String(datasetVersion),cells:Object.freeze(cells.map(cell=>Object.freeze(structuredClone(cell))))})}
export function savePredictionSnapshot(snapshot,{storage=globalThis.localStorage,keyPrefix='koyomi.prediction.snapshot'}={}){if(snapshot?.schemaId!==SNAPSHOT_SCHEMA_ID)throw new TypeError('invalid prediction snapshot');const key=`${keyPrefix}.${snapshot.generatedAt}`;storage.setItem(key,JSON.stringify(snapshot));return key}
export function loadPredictionSnapshot(key,{storage=globalThis.localStorage}={}){const value=storage.getItem(key);return value?Object.freeze(JSON.parse(value)):null}
